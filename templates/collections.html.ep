% layout 'default';

%= include 'filter';

<h3>Kollektionen</h3>

<ol class="collection">
% foreach (@$collection) {
  <li><%= link_to $_->{q}, url_with('collection', coll_id => $_->{id}) %> (<%= $_->{samples} %> Belegstellen)</li>
% }
</ol>
